[^decimal-mode]: Decimal mode allows the processor to compute memory addresses written in decimal values, whereas it otherwise would use hexadecimal. REVIEW THIS FOOTNOTE AND ADD REFERENCES.
[^opcode]: Operation Code, the portion of a machine code instruction that specifies what operation to perform.
[^ALU]: Arithmetic and Logic Unit. In a CPU it is the component responsible for arithmetic and logic operations.
